<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Taverna Limani — Menus</title>
  <meta name="description" content="Taverna Limani — English Menu & Daily Specials" />
  <meta name="theme-color" content="#7B7A4D" />
  <style>
    :root{
      --olive:#7B7A4D;
      --olive-dark:#5f5e3a;
      --ink:#1b1b1b;
      --paper:#ffffff;
      --accent:#4b3b4d;
      --radius:18px;
    }
    *{box-sizing:border-box}
    body{
      margin:0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, "Helvetica Neue", Arial, "Noto Sans", sans-serif;
      color:var(--ink);
      background: linear-gradient(180deg, #f7f7f4 0%, #ffffff 100%);
      display:flex; align-items:center; justify-content:center;
      min-height:100vh;
    }
    .card{
      width:min(720px, 92vw);
      background:var(--paper);
      border-radius:24px;
      box-shadow: 0 10px 30px rgba(0,0,0,.08);
      padding:clamp(20px, 3vw, 36px);
      border: 1px solid rgba(0,0,0,.06);
      text-align:center;
    }
    .brand img{
      width:min(220px, 60%);
      height:auto;
      display:block;
      margin:0 auto 10px;
    }
    h1{font-size:clamp(22px, 3.4vw, 30px); margin:6px 0 2px 0;}
    p.subtitle{margin:0; color:#5a5a5a; font-size:clamp(14px, 2.3vw, 16px)}
    .buttons{display:grid; gap:14px; grid-template-columns:1fr; margin:20px 0}
    @media (min-width: 520px){ .buttons{ grid-template-columns: 1fr 1fr; } }
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      background:var(--olive); color:white; font-weight:600;
      padding:16px 18px; border-radius:var(--radius);
      text-decoration:none; transition:0.2s;
    }
    .btn:hover{ background:var(--olive-dark); }
    .btn-outline{
      background:#fff; color:var(--olive); border:2px solid var(--olive);
    }
    .btn-outline:hover{ border-color:var(--olive-dark); color:var(--olive-dark); }
    .footer{ margin-top:22px; font-size:12px; color:#8a8a8a }
  </style>
</head>
<body>
  <main class="card">
    <section class="brand">
      <img src="REPLACE_WITH_LOGO_URL" alt="Taverna Limani logo" />
      <h1>Welcome to Taverna Limani</h1>
      <p class="subtitle">Choose your menu below</p>
    </section>

    <section class="buttons">
      <a class="btn" href="REPLACE_WITH_ENGLISH_MENU_PDF" target="_blank">English Menu (PDF)</a>
      <a class="btn btn-outline" href="REPLACE_WITH_DAILY_SPECIALS_PDF" target="_blank">Daily Specials (PDF)</a>
    </section>

    <div class="footer">© Taverna Limani — griechisch essen &amp; meer</div>
  </main>
</body>
</html>